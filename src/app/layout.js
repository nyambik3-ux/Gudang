export const metadata = {
  title: 'Gudang System',
  description: 'Sistem Manajemen Gudang',
}

export default function RootLayout({ children }) {
  return (
    <html lang="id">
      <body>{children}</body>
    </html>
  )
}
